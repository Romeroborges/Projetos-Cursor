{
  "name": "bar-clube",
  "private": true,
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm run -w server dev",
    "dev:web": "npm run -w web dev",
    "build": "npm run -w server build",
    "build:web": "npm run -w web build",
    "lint": "npm run -w server lint",
    "db:migrate": "npm run -w server db:migrate",
    "db:seed": "npm run -w server db:seed"
  }
}
